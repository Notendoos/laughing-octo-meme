import { Meta, Story, Canvas } from "@storybook/addon-docs";
import { CustomPhasePanel } from "./CustomPhasePanel";
import { Default } from "./CustomPhasePanel.stories";

<Meta title="Gameplay/Custom Phase Panel" component={CustomPhasePanel} />

# Custom Phase Panel

Shows contextual storytelling for custom breaks (trivia, quick-fire) and lets the player continue the show once they’re ready.

<Canvas>
  <Story name="Default" />
</Canvas>
