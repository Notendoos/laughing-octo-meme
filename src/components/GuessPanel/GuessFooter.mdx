import { Meta, Story, Canvas } from "@storybook/addon-docs";
import GuessFooter from "./GuessFooter";
import { Default } from "./GuessFooter.stories";

<Meta title="Guess Panel/Guess Footer" component={GuessFooter} />

# Guess Footer

Handles remaining queue information and exposes the input row and submit button for new guesses.

<Canvas>
  <Story name="Default" />
</Canvas>
