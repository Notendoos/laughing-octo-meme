import { Meta, Story, Canvas } from "@storybook/addon-docs";
import GuessHeader from "./GuessHeader";
import { Default } from "./GuessHeader.stories";

<Meta title="Guess Panel/Guess Header" component={GuessHeader} />

# Guess Header

Displays remaining guesses, rounds and progress for the word round. It also communicates if the session is paused.

<Canvas>
  <Story name="Default" />
</Canvas>
